general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 6
dependencies:
  pre:
    - npm i -g npm
    - npm i -g codecov nyc
    - npm i js-data@^3.0.1 express
test:
  post:
    - nyc report --reporter=lcov | codecov
