general:
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 5.11.0
dependencies:
  pre:
    - npm i -g npm
    - npm i -g codecov nyc
    - npm i js-data@^3.0.0-rc.4 express
test:
  post:
    - nyc report --reporter=lcov | codecov
